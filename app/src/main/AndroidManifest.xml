<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudySincv2"
        tools:targetApi="31">

        <activity
            android:name=".add_catatan"
            android:exported="false"
            android:parentActivityName=".CActivity"/>
        <activity
            android:name=".add_tugas"
            android:exported="false"
            android:parentActivityName=".LTActivity"/>
        <activity
            android:name=".trash_catatan"
            android:exported="false"
            android:parentActivityName=".CActivity" />
        <activity
            android:name=".trash_tugas"
            android:exported="false"
            android:parentActivityName=".LTActivity" />
        <activity
            android:name=".CActivity"
            android:exported="false" />
        <activity
            android:name=".LTActivity"
            android:exported="false" />
        <activity
            android:name=".StartScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>